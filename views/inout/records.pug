include ../meta.pug
body
    header 
        nav.navbar.navbar-expand-lg.navbar-light.bg-purple
            .container-fluid
                a.navbar-brand Hayakawa-lab Attendance System
                button.navbar-toggler(type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation")
                    span.navbar-toggler-icon
                .collapse.navbar-collapse.justify-content-end#navbarNav
                    ul.navbar-nav
                        li.nav-item
                            a.nav-link(aria-current="page" href="/inout") Home
                        li.nav-item
                            a.nav-link(href="/members/register") メンバーを登録
                        li.nav-item
                            a.nav-link.active(href="/inout/records") ログを見る
    .center
        table.table.table-striped
            thead
                tr
                    - var elements = ['氏名', '日付', '状態']
                    each item in elements
                        th #{item}
            tbody
                each person in record
                    tr
                        th #{person.name}
                        th #{person.time}
                        th #{person.state}